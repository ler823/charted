AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: >
  charted-backend

  Backend for Charted

Globals:
  Function:
    Timeout: 10
    Environment:
      Variables:
        BUCKET_NAME: !Ref ChartedPhotoBucket

Resources:
  ChartedPhotoBucket:
    Type: AWS::S3::Bucket
    Properties:
      CorsConfiguration:
        CorsRules:
          - AllowedOrigins:
              - "*"
            AllowedMethods:
              - PUT
            AllowedHeaders:
              - "*"
  UploadPhotoUrl:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: upload_photo_url/
      Handler: app.lambda_handler
      Runtime: python3.14
      Architectures:
        - arm64
      Policies:
        - S3WritePolicy:
            BucketName: !Ref ChartedPhotoBucket
      Events:
        GenerateUploadPhotoUrl:
          Type: HttpApi
          Properties:
            Path: /uploadphotourl
            Method: post

Outputs:
  UploadPhotoUrlApi:
    Description: "API Gateway endpoint URL for Prod stage for Get Pin function"
    Value: !Sub "https://${ServerlessHttpApi}.execute-api.${AWS::Region}.${AWS::URLSuffix}/uploadphotourl"
